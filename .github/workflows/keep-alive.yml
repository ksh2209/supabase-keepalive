name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 0 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s "https://cosolvvjqgfclamictov.supabase.co/rest/v1/access_codes?select=code&limit=1" \
            -H "apikey: sb_publishable_ofthzWmaRhrrMOZU2Rk6AA_Qpqi5e4a" \
            -H "Authorization: Bearer sb_publishable_ofthzWmaRhrrMOZU2Rk6AA_Qpqi5e4a" \
            -o /dev/null
          echo "Supabase ping 완료"
